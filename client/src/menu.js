function Menu() {
    setup() {
        // this.button = createButton('click me');
        // this.button.position(0, 0);
        // this.button.mousePressed(this.startGame());
    }


    // mgr.showNextScene()

    draw() {
        background(42);
    }
}